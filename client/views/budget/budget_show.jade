template(name="BudgetShow")
  .container
    .row
      .col-xs-12
        table.table
          thead
            tr
              th {{i18n 'product.description' }}
              th {{i18n 'product.price_without_tax' }}
              th {{i18n 'product.quantity' }}
              th {{i18n 'product.total_without_tax' }}
          tbody
            +each products
              tr
                td {{ description }}
                td {{ price }}
                td {{ quantity }}
                td {{ total }}
            tr
              td
              td
              td
                b Total s/IVA:
              td
                b {{ budgetTotal }}€
